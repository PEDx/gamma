import { useEffect, FC } from 'react';
import { Box, Input } from '@chakra-ui/react';

export const StringInput: FC = () => {
  useEffect(() => {}, []);
  return (
    <Box>
      <Input size="xs" />
    </Box>
  );
};
