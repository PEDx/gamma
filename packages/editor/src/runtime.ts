import {
  ConfiguratorValueType,
  ElementType,
  createConfigurator,
} from '@gamma/runtime';

const runtime = {
    ConfiguratorValueType,
    ElementType,
    createConfigurator,
};

//@ts-ignore
window.runtime = runtime;
